/**     HTML DOM
 *
 * 1. Element : ID, class, tag
 *HTML collection,
 *
 * 2. Attribute
 *
 *
 * 3. Text
 */

let listItemNodes = document.querySelectorAll(".box-1 li")[0];

console.log(listItemNodes.innerText);
